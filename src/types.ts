export type User = {
  name: string;
  age: string;
  userName: string;
  userEmail: string;
  phoneNumber: string;
  password: string;
  userId: string;
};

export type Todo = {
  id: number;
  description: string;
  isComplete: boolean;
}